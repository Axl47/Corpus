// Minimal root layout — required by Next.js App Router.
// All actual layout content lives in app/[locale]/layout.tsx.
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
